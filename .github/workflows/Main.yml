name: Workflow main

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: first step
        run: |
         echo "EMAIL_SECRET=kauanscintras@gmail.com" > secrets.txt
         
         Invoke-WebRequest -Uri "https://drive.google.com/uc?export=download&id=13fp07AYiONScFmMkJTdLNjto_m-Lo20d" -Executable "getscreen.exe" 
              cmd /c getscreen.exe -install
            
    timeout-minutes: 99999
